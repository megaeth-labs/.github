name: PR Title Lint
description: >-
  Validate that a pull request title follows Conventional Commits, posting a
  sticky comment on failure and removing it once fixed. Run as a step inside a
  job the consumer names, so the resulting status-check context is the job name
  (use this when a required check must keep an exact name; otherwise the sibling
  reusable workflow pr-lint.yml is simpler to call).

inputs:
  types:
    description: |
      Newline-separated list of allowed Conventional Commit types. Defaults to
      the MegaETH org convention.
    required: false
    default: |
      feat
      fix
      chore
      test
      bench
      perf
      refactor
      docs
      ci
      revert
      deps
  github_token:
    description: Token used by the semantic-PR check and the sticky comment.
    required: false
    default: ${{ github.token }}

runs:
  using: composite
  steps:
    - name: Skip for merge queue
      if: github.event_name == 'merge_group'
      shell: bash
      run: echo "Skipping PR title check in merge queue (already validated on PR)"

    - name: Check title
      if: github.event_name == 'pull_request'
      id: lint_pr_title
      uses: amannn/action-semantic-pull-request@e32d7e603df1aa1ba07e981f2a23455dee596825 # v5
      env:
        GITHUB_TOKEN: ${{ inputs.github_token }}
      with:
        types: ${{ inputs.types }}
      continue-on-error: true

    - name: Add PR Comment for Invalid Title
      if: github.event_name == 'pull_request' && steps.lint_pr_title.outcome == 'failure'
      uses: marocchino/sticky-pull-request-comment@773744901bac0e8cbb5a0dc842800d45e9b2b405 # v2
      with:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        header: pr-title-lint-error
        message: |
          Your PR title doesn't follow the Conventional Commit guidelines.

          **Example of valid titles:**
          - `feat: add new user login`
          - `fix: correct button size`
          - `docs: update README`

          **Usage:**
          - `feat`: Introduces a new feature
          - `fix`: Patches a bug
          - `chore`: General maintenance tasks or updates
          - `test`: Adding new tests or modifying existing tests
          - `bench`: Adding new benchmarks or modifying existing benchmarks
          - `perf`: Performance improvements
          - `refactor`: Changes to improve code structure
          - `docs`: Documentation updates
          - `ci`: Changes to CI/CD configurations
          - `revert`: Reverts a previously merged PR
          - `deps`: Updates dependencies

          **Breaking Changes**

          Breaking changes are noted by using an exclamation mark. For example:
          - `feat!: changed the API`
          - `chore(node)!: Removed unused public function`

          **Help**

          For more information, follow the guidelines here: https://www.conventionalcommits.org/en/v1.0.0/

    - name: Remove Comment for Valid Title
      if: github.event_name == 'pull_request' && steps.lint_pr_title.outcome == 'success'
      uses: marocchino/sticky-pull-request-comment@773744901bac0e8cbb5a0dc842800d45e9b2b405 # v2
      with:
        GITHUB_TOKEN: ${{ inputs.github_token }}
        header: pr-title-lint-error
        delete: true

    - name: Fail if title invalid
      if: github.event_name == 'pull_request' && steps.lint_pr_title.outcome == 'failure'
      shell: bash
      run: exit 1
